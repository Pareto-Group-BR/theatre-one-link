# theatre-one-link
Theatre Harness em um link: landing e skill auto-contida para Claude Web/Desktop.

O `index.html` é um espelho byte a byte de `Pareto-Group-BR/theatre-harness:public/index.html` da última entrega aprovada. A atualização atual publica `main@4d097023`, incluindo o caminho **Abrir no Claude Desktop** com o mesmo prompt integral do caminho Web.
